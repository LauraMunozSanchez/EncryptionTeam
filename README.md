# USO DE ESTE REPOSITORIO
Se compone de la siguiente estructura:
* Una carpeta llamada **encryptedMessages**  donde se subiran los archivos encriptados que queramos compartir.
* Una carpeta llamada **publicKeys**.
    * Dentro de esta habrán carpetas con el nombre de cada con sus claves públicas.
